pipeline {
    agent any
    
    stages{
        stage('build'){
            steps{
                bat "mvn clean compile"
            }
        }
        stage('test'){
            steps{
                bat "mvn test"
            }
            post {
                always {
                    junit 'target/surefire-reports/*.xml'
                }
            }
        }
    }

}