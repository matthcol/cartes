pipeline {
    agent { 
    	node { 
    		label 'master'
    		env.JAVA_HOME="${tool 'jdk8'}"
    		env.PATH="${env.JAVA_HOME}/bin:${env.PATH}"
    	}
    }
    
    stages{
        stage('build'){
            steps{
                bat "mvn clean compile"
            }
        }
    }

}