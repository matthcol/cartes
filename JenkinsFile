pipeline {
    agent { 
    	label 'master'
    }
    
    
    jdk = tool name :'jdk8'
    env.JAVA_HOME = ${jdk}
    
    
    stages{
        stage('build'){
            steps{
                bat "mvn clean compile"
            }
        }
    }

}